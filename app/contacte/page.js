"use client";
import {useSearchParams} from "next/navigation";
import {Suspense,useState} from "react";
import apps from "../../data/apps.json";
function ContactForm(){const params=useSearchParams();const appParam=params.get("app")||"";const [sent,setSent]=useState(false);if(sent)return(<div className="contact-wrap" style={{textAlign:"center",paddingTop:"5rem"}}><h1>Enviat!</h1><p style={{color:"var(--muted)",margin:"1rem 0 2rem"}}>Et respondrem aviat.</p><a href="/" className="btn btn-outline">Inici</a></div>);return(<div className="contact-wrap"><h1>Demana una demo</h1><form onSubmit={e=>{e.preventDefault();setSent(true);}}><div className="form-group"><label>Nom *</label><input type="text" required placeholder="Anna Garcia"/></div><div className="form-group"><label>Centre educatiu *</label><input type="text" required placeholder="Institut Escola Industrial"/></div><div className="form-group"><label>Correu *</label><input type="email" required placeholder="hola@escola.cat"/></div><div className="form-group"><label>Projecte d interes</label><select defaultValue={appParam}><option value="">Tots</option>{apps.map(a=><option key={a.slug} value={a.nom}>{a.nom}</option>)}</select></div><div className="form-group"><label>Missatge</label><textarea rows={4}/></div><button type="submit" className="btn-submit">Envia</button></form></div>);}
export default function ContactePage(){return <Suspense fallback={<div/>}><ContactForm/></Suspense>;}
